#!/usr/bin/env bash
CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$CURRENT_DIR/helpers.sh"

# main() {
#   if [[ -n "$1" ]]; then
#     restore_session "$1"
#   else
#     restore_last
#   fi
# }
# main "$@"
